# Optional: set server-level API keys (users can still BYOK via the Settings page if omitted)
ANTHROPIC_API_KEY=
OPENAI_API_KEY=

# Database path (default: ./data/research-ai.db)
# When deploying with persistent volumes, set to e.g. /app/data/research-ai.db
DATABASE_PATH=
